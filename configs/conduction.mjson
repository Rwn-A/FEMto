name = "my_sim"

mesh = "../meshes/part.msh"

model = Conduction

fields = {
    temperature = {
        order = Linear,
        boundaries = {
            "left" = {type = "isothermal", temperature = 100},
            "right" = {type = "isothermal", temperature = 200}
        }
    }
}

output = {
    directory = "../output"
}